<?xml version="1.0"?>
<ruleset name="WordPress Theme Coding Standards">
	<!-- See https://github.com/squizlabs/PHP_CodeSniffer/wiki/Annotated-ruleset.xml -->
	<!-- See https://github.com/WordPress/WordPress-Coding-Standards -->
	<!-- See https://github.com/WPTRT/WPThemeReview -->
	<!-- See https://github.com/PHPCompatibility/PHPCompatibilityWP -->

	<!-- Set a description for this ruleset. -->
	<description>A custom set of code standard rules to check for WordPress themes.</description>

	<arg value="ps"/>

	<!-- Strip the filepaths down to the relevant bit. -->
	<arg name="basepath" value="./"/>

	<!-- Check up to 8 files simultaneously. -->
	<arg name="parallel" value="8"/>

	<!-- Check PHP files only. JavaScript and CSS files are checked separately using the @wordpress/scripts package. -->
	<arg name="extensions" value="php"/>

	<!-- Check all files in this directory and the directories below it. -->
	<file>.</file>

	<!-- Exclude patterns. -->
	<exclude-pattern>/vendor/*</exclude-pattern>
	<exclude-pattern>/node_modules/*</exclude-pattern>

	<rule ref="WordPress">
		<!-- Generic excludes -->
		<exclude name="Generic.Files.LineEndings.InvalidEOLChar" />
		<exclude name="WordPress.WP.EnqueuedResources.NonEnqueuedStylesheet" />
		<exclude name="WordPress.WP.EnqueuedResources.NonEnqueuedScript" />
		<exclude name="WordPress.WP.GlobalVariablesOverride.Prohibited" />

		<!-- Exclude some of the rules related to commenting -->
		<exclude name="Squiz.Commenting.FileComment.Missing" />
		<exclude name="Squiz.Commenting.FunctionComment.ParamCommentFullStop" />
		<exclude name="Squiz.Commenting.InlineComment.InvalidEndChar" />
		<exclude name="Squiz.PHP.CommentedOutCode.Found" />

		<!-- Exclude some of the rules related to variable naming/prefixing -->
		<exclude name="WordPress.NamingConventions.PrefixAllGlobals.NonPrefixedVariableFound" />

		<!-- Exclude some of the rules related to queries (meta query, tax query) -->
		<exclude name="WordPress.DB.SlowDBQuery.slow_db_query_tax_query" />
		<exclude name="WordPress.DB.SlowDBQuery.slow_db_query_meta_value" />
		<exclude name="WordPress.DB.SlowDBQuery.slow_db_query_meta_key" />
	</rule>

	<rule ref="WPThemeReview">
		<exclude name="PHPCompatibility"/>
		<!-- <exclude name="WPThemeReview.PluginTerritory.NoAddAdminPages.add_menu_pages_add_menu_page" />
		<exclude name="WPThemeReview.PluginTerritory.NoAddAdminPages.add_menu_pages_add_submenu_page" />
		<exclude name="WPThemeReview.CoreFunctionality.FileInclude.FileIncludeFound" />
		<exclude name="WPThemeReview.CoreFunctionality.PostsPerPage.posts_per_page_posts_per_page" />
		<exclude name="WPThemeReview.CoreFunctionality.PostsPerPage.posts_per_page_numberposts" /> -->
	</rule>

	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array" value="groteski" />
		</properties>
	</rule>

	<rule ref="WordPress.Files.FileName">
		<properties>
			<!-- <property name="is_theme" value="true" /> -->
		</properties>
	</rule>

	<config name="minimum_supported_wp_version" value="6.0" />

	<rule ref="WordPress.Arrays.MultipleStatementAlignment">
		<properties>
			<!-- <property name="exact" value="false" />
			<property name="alignMultilineItems" value="!=100" />
			<property name="ignoreNewlines" value="false "/> -->
		</properties>
	</rule>

	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="groteski" />
			</property>
		</properties>
	</rule>
</ruleset>
